plugins {
    id 'java'
}

group = 'com.dotran.oms'
version = project.findProperty("version")

repositories {
    mavenCentral()
}

dependencies {
    api project(':core-common')
}

test {
    useJUnitPlatform()
}